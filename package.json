{
    "scripts": {
        "dev:all": "concurrently \"set PYTHONIOENCODING=utf-8 && uvicorn main_api:app --app-dir backend --reload --port 8000\" \"npm run dev --prefix frontend\""
    },
    "devDependencies": {
        "concurrently": "^10.0.5"
    }
}
